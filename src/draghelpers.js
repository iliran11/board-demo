export function itemResort(result, state) {
  // const nextState = { ...state };
  // const { destination, source } = result;
  // const sourceSection = source.droppableId;
  // const destinationSection = destination.droppableId;
  // const sourceList = getListOfSection(nextState, sourceSection);
  // const destinationList = getListOfSection(nextState, destinationSection);
  // const draggedItem = result.draggableId;

  // [removed] = nextState[sourceList][sourceSection].splice(source.index);
  // console.log(result);
}

// function getListOfSection(state, sectionId) {
//   const stateKeys = Object.keys(state);
//   const result = stateKeys.filter(listKey => {
//     const arrayOfSections = state[listKey];
//     return arrayOfSections.some(section => {
//       return section.id === sectionId;
//     });
//   });
// }
